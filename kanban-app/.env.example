# Backend
PORT=3001
CLIENT_URL=http://159.195.34.237:44215

# SQLite database path (Docker sets this to /data/kanban.db via docker-compose.yml)
# Leave empty for local dev – defaults to backend/kanban.db
DB_PATH=
